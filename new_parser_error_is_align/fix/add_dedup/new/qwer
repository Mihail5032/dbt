# 1. Удали submodule-ссылку
git rm --cached wpufib-output-adapter/src

# 2. Удали .git внутри src (из-за него git думал что это submodule)
rm -rf wpufib-output-adapter/src/.git

# 3. Добавь src как обычную папку
git add wpufib-output-adapter/src/

# 4. Закоммить и запушь
git commit -m "Fix: добавить wpufib-output-adapter/src как обычную папку"
git push

